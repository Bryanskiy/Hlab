fn fib(n) {

    first = 1;
    second = 1;
    iter = 2;
    while ( iter < n ) {
        tmp = second;
        second = second + first;
        first = tmp;
        iter = iter + 1;
    }

    return second;
}

fn main() {
    n = ?;
    ans = fib(n);
    print ans;
    return 0;
}
fn main() {
    current_x = 5;
    current_y = 6;

    while(current_x <= 5) {

        while(current_y <= 12) {
            print current_y;
            current_y = current_y + 1;
        }

        current_x = current_x + 1;
    }

    return 0;
}